{
  "rules": {
    ".read": "true",
    ".validate": "newData.hasChildren(['broadcasts'])",

    "broadcasts": {
      ".write": "true",

      "$broadcastId": {
        ".validate": "newData.hasChildren(['createdAt', 'active', 'lastActive', 'lat', 'long', 'courseCode', 'description'])",

        "createdAt": {
          ".validate": "newData.isNumber() && newData.val() <= now"
        },
        "active": {
          ".validate": "newData.isBoolean()"
        },
        "lastActive": {
          ".validate": "newData.isNumber() && newData.val() <= now"
        },
        "lat": {
          ".validate": "newData.isNumber()"
        },
        "long": {
          ".validate": "newData.isNumber()"
        },
        "courseCode": {
          ".validate": "newData.isString()"
        },
        "description": {
          ".validate": "newData.isString()"
        }
      }
    }
  }
}